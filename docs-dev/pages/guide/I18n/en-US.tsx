export default {
  i18n: {
    title: 'i18n Internationalization',
    intro: 'solid-mobile ships with zh-CN / en-US bilingual support. Component runtime text switches automatically via locale. For business copy, use useT() or plug in any third-party i18n solution.',
    downloadTitle: 'Download Template',
    downloadDesc: 'Click the button below to download the component runtime dictionary template (JSON). Contains only strings actually used by components (button labels, placeholders, status hints from Picker, Dialog, Calendar, etc.).',
    downloadSteps: 'Steps:',
    downloadStep1: '1. Download template → 2. Replace English values with your target language → 3. Pass via localeMessages',
    downloadNote: "Note: The JSON structure must be preserved. Remove fields you don't need (missing keys fall back to English). The nesting hierarchy must remain intact (e.g. component.picker.cancel must be { component: { picker: { cancel: \"...\" } } }).",
    downloadBtn: 'Download English Template (JSON)',
    basicTitle: 'Basic Usage',
    overrideTitle: 'Override Built-in Entries',
    overrideDesc: 'You can also override specific keys in zh-CN / en-US. User values win on conflict.',
    fallbackTitle: 'Fallback Chain',
    fallbackDesc: 'When a key is missing in the current locale, the following chain is used:',
    fallback1: 'Current locale (e.g. ja-JP, including user-provided dictionary)',
    fallback2: 'en-US (built-in English, as universal fallback)',
    fallback3: 'Returns the key path (e.g. component.picker.confirm), with a console.warn emitted (once per key)',
    fallbackNote: "This means you don't need to translate every key at once — provide what you have, the rest falls back to English. The console warning helps you quickly find which keys still need coverage.",
    dictTitle: 'Dictionary Structure',
    dictDesc: 'Here is the top-level structure of the built-in dictionary. You can override any level as needed:',
    progTitle: 'Programmatic API',
    progDesc: 'Besides the ProviderConfig prop, you can call setUserMessages() at the app entry:',
  },
};
